{
  "name": "fishing-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:db": "docker compose up -d postgres",
    "db:migrate": "npx prisma migrate dev",
    "db:reset": "npx prisma migrate reset",
    "db:studio": "npx prisma studio",
    "db:generate": "npx prisma generate",
    "db:seed": "npx tsx prisma/seed.ts",
    "db:seed:users": "npx tsx -e \"import { PrismaClient } from '@prisma/client'; import { seedUsers } from './prisma/src/seed-user'; const prisma = new PrismaClient(); seedUsers(prisma).then(() => prisma.$disconnect())\"",
    "db:seed:posts": "npx tsx -e \"import { PrismaClient } from '@prisma/client'; import { seedPosts } from './prisma/src/seed-post'; const prisma = new PrismaClient(); prisma.user.findMany().then(users => seedPosts(prisma, users)).then(() => prisma.$disconnect())\"",
    "db:seed:follows": "npx tsx -e \"import { PrismaClient } from '@prisma/client'; import { seedFollows } from './prisma/src/seed-follow'; const prisma = new PrismaClient(); prisma.user.findMany().then(users => seedFollows(prisma, users)).then(() => prisma.$disconnect())\"",
    "db:seed:likes": "npx tsx -e \"import { PrismaClient } from '@prisma/client'; import { seedLikes } from './prisma/src/seed-like'; const prisma = new PrismaClient(); Promise.all([prisma.user.findMany(), prisma.post.findMany()]).then(([users, posts]) => seedLikes(prisma, users, posts)).then(() => prisma.$disconnect())\""
  },
  "dependencies": {
    "@aws-amplify/ui-react": "^6.11.2",
    "@aws-sdk/client-cognito-identity-provider": "^3.844.0",
    "@emotion/react": "^11.11.3",
    "@emotion/styled": "^11.11.0",
    "@googlemaps/js-api-loader": "^1.16.10",
    "@mui/icons-material": "^5.15.10",
    "@mui/material": "^5.15.10",
    "@prisma/client": "^6.11.1",
    "@types/google.maps": "^3.58.1",
    "@types/sass": "^1.43.1",
    "aws-amplify": "^6.15.3",
    "next": "15.3.5",
    "prisma": "^6.11.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sass": "^1.89.2",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.3.5",
    "tailwindcss": "^4",
    "tsx": "^4.20.4",
    "typescript": "^5"
  }
}
